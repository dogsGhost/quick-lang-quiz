import expect from 'expect';
import db from './../js/db';

describe('db', () => {
  let expectedProps = ['pronoun', 'verb', 'determiner', 'noun'];

  it('should contain the expected props');

  it('each prop should be an array of objects');
});
